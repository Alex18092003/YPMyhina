<Page x:Class="YPMyhina.PageProductList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:YPMyhina"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Список товаров">
    <Page.Resources>
        <BitmapImage x:Key="defaultImage" UriSource="../Resorces/pocture.png"></BitmapImage>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"></RowDefinition>
            <RowDefinition Height="2*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"  Orientation="Horizontal">
      
            <Image  Width="100" Height="100" Source="../Resorces/logo.png" HorizontalAlignment="Left"></Image>
            <TextBlock>Товары</TextBlock>
            <Button Name="ButtonExit" Click="ButtonExit_Click">Выход</Button>
            <TextBlock Name="TextFIO"></TextBlock>
        </StackPanel>
       
        <StackPanel Grid.Row="1">
            <ListView Height="300" Name="ListProduct" ScrollViewer.HorizontalScrollBarVisibility="Disabled" >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel >
                            
                        </StackPanel>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="GreenYellow" BorderThickness="2">
                            <Grid>
                                <StackPanel Orientation="Horizontal">
                                    <Image Width="100" Height="100" Name="ImageProduct" Source="{Binding ProductImage, TargetNullValue={StaticResource defaultImage}}"></Image>
                                    <StackPanel>
                                        <TextBlock Text="{Binding ProductName}" Name="TextName"></TextBlock>
                                        <TextBlock Text="{Binding ProductDescription}" Name="TextDescription"></TextBlock>
                                        <TextBlock Text="{Binding Manufacturer.ManufacturerName}" Name="TextManufacturer"></TextBlock>
                                        <TextBlock Text="{Binding ProductCost}" Name="TextPrice"></TextBlock>
                                    </StackPanel>
                                    <TextBlock Text="{Binding ProductDiscountAmount}" Name="Text"></TextBlock>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</Page>
